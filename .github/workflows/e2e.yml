# E2E 테스트 — main PR (release/hotfix) + 수동 실행
# 프로덕션 배포 전 "화면이 실제로 동작하는가" 최종 검증
name: E2E 테스트

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  e2e-test:
    name: E2E 테스트
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: uv 설치
        uses: astral-sh/setup-uv@v4
        with:
          version: "0.5.20"
          enable-cache: true

      - name: Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Node.js 설정
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: BE 의존성 설치
        run: uv sync --all-extras

      - name: DB 마이그레이션
        env:
          DATABASE_URL: sqlite+aiosqlite:///./e2e_test.db
        run: PYTHONPATH=backend uv run alembic -c backend/alembic.ini upgrade head

      - name: BE 서버 시작
        env:
          DATABASE_URL: sqlite+aiosqlite:///./e2e_test.db
          SECRET_KEY: test-secret-key  # pragma: allowlist secret
          JWT_SECRET: e2e-jwt-secret  # pragma: allowlist secret
          LLM_PROVIDER: anthropic
          ANTHROPIC_API_KEY: test-key  # pragma: allowlist secret
          DEBUG: "true"
        run: |
          PYTHONPATH=backend uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 &
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8000/health > /dev/null 2>&1; then
              echo "BE 준비 완료 (${i}초)"
              break
            fi
            sleep 1
          done
          curl -f http://localhost:8000/health || exit 1

      - name: FE 빌드 + 서빙
        run: |
          cd frontend && npm ci && npm run build
          npx serve dist -l 5173 -s &
          sleep 2
        env:
          VITE_API_URL: http://localhost:8000/api

      - name: E2E 의존성 설치
        run: npm ci

      - name: Playwright 브라우저 설치
        run: npx playwright install --with-deps chromium

      - name: E2E 테스트 실행
        env:
          E2E_BASE_URL: http://localhost:5173
          E2E_API_URL: http://localhost:8000
          E2E_JWT_SECRET: e2e-jwt-secret  # pragma: allowlist secret
        run: npx playwright test --config e2e/playwright.config.ts

      - name: 테스트 실패 리포트 업로드
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            test-results/
            playwright-report/
          retention-days: 7

      - name: 텔레그램 알림
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ "${{ job.status }}" = "success" ]; then
            MESSAGE="🎭 E2E 테스트 통과 ✅ — main PR 준비 완료"
          else
            MESSAGE="🎭 E2E 테스트 실패 ❌ — main 머지 차단"
            MESSAGE="${MESSAGE}"$'\n'"🔗 https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          fi
          curl -sf -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=${MESSAGE}" || true
