# 포도가계부(Podo Budget) 리브랜딩 설계

> **상태:** 승인됨 (2026-02-16)

## 개요

HomeNRich를 포도가계부(Podo Budget)로 리브랜딩한다. 포도책방과 동일한 디자인 시스템(grape 컬러, cream 배경, 성장 메타포)을 적용하여 가족 앱 시리즈로 통일감을 만든다.

## 1. 이름 & 브랜딩

| 항목 | 현재 | 변경 |
|------|------|------|
| 프로젝트명 | HomeNRich / homenrich | **Podo Budget / podo-budget** |
| 한국어명 | - | **포도가계부** |
| 도메인 | homenrich-*.fly.dev | **podobudget.com** (추후 확보) |
| 브랜드 심볼 | 없음 | 🍇 포도 이모지 |
| 태그라인 | - | "포도알처럼 하나씩, 알찬 가계부" |

변경 대상:
- `package.json` name 필드
- HTML `<title>`, PWA manifest (name, short_name)
- 사이드바/헤더 로고 텍스트
- GitHub repo description (추후)
- Fly.io 앱 이름 (추후)

## 2. 컬러 시스템

포도책방의 토큰을 그대로 가져온다.

### 팔레트 정의 (index.css @theme)

```css
/* Grape Purple (Primary) */
--color-grape-50:  #faf5ff
--color-grape-100: #f3e8ff
--color-grape-200: #e9d5ff
--color-grape-300: #d8b4fe
--color-grape-400: #c084fc
--color-grape-500: #a855f7
--color-grape-600: #9333ea
--color-grape-700: #7c3aed
--color-grape-800: #6b21a8
--color-grape-900: #581c87

/* Leaf Green (Accent) */
--color-leaf-50:  #f0fdf4
--color-leaf-100: #dcfce7
--color-leaf-200: #bbf7d0
--color-leaf-400: #4ade80
--color-leaf-500: #22c55e
--color-leaf-600: #16a34a
--color-leaf-700: #15803d

/* Warm Neutrals */
--color-cream:    #fefce8
--color-warm-50:  #fafaf9
--color-warm-100: #f5f5f4
--color-warm-200: #e7e5e4
--color-warm-500: #78716c
--color-warm-700: #44403c
--color-warm-900: #1c1917
```

### 역할 매핑

| 용도 | 현재 | 변경 |
|------|------|------|
| Primary (버튼, 강조) | `amber-*` | `grape-*` |
| 지출 표시 | `amber-*` | `grape-*` |
| 수입 표시 | `emerald-*` | `leaf-*` |
| 텍스트/보더 | `stone-*` | `warm-*` |
| 페이지 배경 | `white` | `cream` |
| 에러/경고 | `red-*` | `red-*` (유지) |

### 애니메이션 (포도책방에서 가져옴)

```css
@keyframes grape-pop {
  0% { transform: scale(0); opacity: 0; }
  60% { transform: scale(1.2); }
  100% { transform: scale(1); opacity: 1; }
}

@keyframes bounce-in {
  0% { transform: translateY(20px); opacity: 0; }
  60% { transform: translateY(-5px); }
  100% { transform: translateY(0); opacity: 1; }
}
```

### 클래스 치환 규칙

- `amber-*` → `grape-*` (모든 shade)
- `emerald-*` → `leaf-*` (모든 shade)
- `stone-*` → `warm-*` (모든 shade)
- 페이지 레벨 `bg-white` → `bg-cream`

## 3. 성장 메타포 시스템

### 단계

| 단계 | 이름 | 조건 | 시각 표현 |
|------|------|------|-----------|
| 1 | 포도알 | 거래 1건 기록 | 보라색 원 1개 |
| 2 | 포도송이 | 포도알 10개 (거래 10건) | 포도송이 SVG |
| 3 | 포도나무 | 포도송이 10개 (거래 100건) | 나무 SVG (추후) |
| 4 | 포도정원 | 포도나무 여러 그루 | 정원 풍경 (추후) |

### UI 적용 위치

1. **대시보드 상단 카드** - 현재 성장 상태
   - "이번 달 포도알 7개 / 포도송이까지 3개 남았어요"
   - 포도알 10개 원형 배열: 채워진 것 `grape-700`, 빈 것 `grape-200`
   - `grape-pop` 애니메이션 (새 거래 등록 시)

2. **사이드바 하단** - 누적 성장 요약
   - 🍇 아이콘 + "포도알 47개" 텍스트

3. **거래 등록 완료 시** - 토스트 메시지
   - "🍇 포도알 +1! (이번 달 8/10)"

### 데이터 소스

별도 모델 없음. 기존 Expense + Income 건수를 count하여 계산. 프론트엔드에서 월별 통계 API의 `count` 필드 활용.

### 구현 범위

이번: 이번 달 기준 포도알/송이 표시. 추후: 포도나무/정원은 누적 데이터 충분히 쌓인 후 확장.

## 4. 변경 범위

### 프론트엔드

| 영역 | 파일 수 | 작업 |
|------|---------|------|
| CSS 테마 | 1 | `index.css` @theme 전면 교체 |
| 컬러 클래스 치환 | ~20 | 모든 페이지/컴포넌트 amber→grape, emerald→leaf, stone→warm |
| 브랜딩 텍스트 | ~5 | Layout 헤더, PWA manifest, index.html title |
| 성장 메타포 UI | ~3 | 대시보드 카드, 사이드바 요약, 토스트 |
| 테스트 업데이트 | ~10 | 변경된 텍스트/클래스 반영 |

### 백엔드

변경 없음. 순수 FE 리브랜딩.

### 검증

- `grep -r "amber\|emerald\|stone-" src/` 로 치환 누락 전수검사
- `npm test -- --run` 전체 테스트 통과 확인
- `npm run build` 빌드 성공 확인
