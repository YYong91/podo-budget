# ──────────────── Database ────────────────
# Docker 사용 시: postgresql+asyncpg://homenrich:homenrich@db:5432/homenrich  # pragma: allowlist secret
# 로컬 개발 시:   postgresql+asyncpg://homenrich:homenrich@localhost:5432/homenrich  # pragma: allowlist secret
DATABASE_URL=postgresql+asyncpg://homenrich:homenrich@localhost:5432/homenrich  # pragma: allowlist secret
# 외부 관리형 DB(Supabase 등) 사용 시 True
DATABASE_SSL=False

# ──────────────── LLM ────────────────
# 기본 프로바이더: anthropic | openai | google | local
LLM_PROVIDER=anthropic
# 기본 모델 (빈 문자열이면 프로바이더 기본 모델 사용)
LLM_MODEL=

# 기능별 오버라이드 (설정하면 해당 기능에만 적용, 미설정 시 위 기본값 사용)
# 자연어 파싱용
LLM_PROVIDER_PARSE=
LLM_MODEL_PARSE=
# 인사이트 분석용
LLM_PROVIDER_INSIGHTS=
LLM_MODEL_INSIGHTS=
# OCR용
LLM_PROVIDER_OCR=
LLM_MODEL_OCR=

# API Keys (사용할 프로바이더에 맞는 키만 설정하면 됩니다)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
GOOGLE_API_KEY=your_google_api_key_here

# ──────────────── Telegram Bot ────────────────
# @BotFather에서 /newbot으로 생성 후 발급받은 토큰
# Webhook 설정: https://api.telegram.org/bot{TOKEN}/setWebhook?url={YOUR_DOMAIN}/api/telegram/webhook&secret_token={SECRET}
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
# Webhook 보안 시크릿 (setWebhook의 secret_token과 동일하게 설정)
TELEGRAM_WEBHOOK_SECRET=your_webhook_secret_here

# ──────────────── KakaoTalk Bot ────────────────
# 카카오 i 오픈빌더 > 설정 > API KEY에서 발급
# Webhook 보안 검증에도 사용됨 (Authorization 헤더)
KAKAO_BOT_API_KEY=your_kakao_bot_api_key_here

# ──────────────── Email (Resend) ────────────────
# 빈 문자열이면 이메일 발송 비활성화
RESEND_API_KEY=
# 발신자 이메일 주소 (Resend에서 인증된 도메인 필요)
RESEND_FROM_EMAIL=HomeNRich <noreply@homenrich.app>

# ──────────────── Sentry 에러 트래킹 ────────────────
# DSN 미설정 시 완전 비활성화 (안전)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development

# ──────────────── CORS ────────────────
# 허용할 프론트엔드 오리진 (쉼표로 구분)
# 프로덕션 시 실제 도메인으로 변경 필수
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ──────────────── Application ────────────────
APP_NAME=HomeNRich
# 프로덕션에서는 반드시 False로 설정
DEBUG=True
# 프로덕션에서는 반드시 강력한 무작위 문자열로 변경 (예: openssl rand -hex 32)
SECRET_KEY=your_secret_key_here
